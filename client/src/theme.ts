import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#DCFCE7',
    },
    secondary: {
      main: '#21C65E',
    },
    background: {
      default: '#f0fdf4',
    },
  },
});

export default theme;
