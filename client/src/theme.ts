import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#30cf69',
    },
    secondary: {
      main: '#21C65E',
    },
    background: {
      default: "#f0fdf4",
    },
  },
});

export default theme;
